## Specs: tighten-eslint-config

This change has no new or modified capabilities. It is an engineering quality improvement with zero product behavior changes.

- **Backend**: Fix typing, remove disabled ESLint rules, eliminate `any` usage
- **Frontend**: Fix inline lint suppressions, replace `<img>`, scope hook rules to React files
